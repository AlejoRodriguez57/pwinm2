export function mapOperaciones(operaciones) {
    return operaciones.map(item => item.nombre);
}

export function mapEstados(estados) {
    return estados.map(item => item.nombre);
}

export function mapTiposPropiedad(tipos) {
    return tipos.map(item => item.nombre);
}

export function mapUbicaciones(ubicaciones) {
    return ubicaciones.map(item => item.nombre);
}